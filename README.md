# goit-algo-hw-05
Basic Algorithms

# Порівняння ефективності алгоритмів пошуку підрядка

Цей проєкт реалізує та порівнює ефективність трьох популярних алгоритмів пошуку підрядка в тексті: **Кнута-Морріса-Пратта (KMP)**, **Боєра-Мура** та **Рабіна-Карпа**.

Порівняння проводиться на основі двох реальних текстових файлів (статей) шляхом вимірювання часу виконання пошуку існуючого та неіснуючого підрядків.


## 🧩 Опис алгоритмів

### 1. Алгоритм Кнута-Морріса-Пратта (KMP)
Використовує префікс-функцію (LPS масив), щоб уникнути зайвих порівнянь символів. Коли виникає розбіжність, алгоритм знає, на скільки символів можна безпечно зсунутись, не повертаючись назад у тексті.
* **Складність:** $O(N + M)$

### 2. Алгоритм Боєра-Мура
Вважається одним з найшвидших для звичайного тексту. Він будує таблицю зсувів і починає порівняння з кінця патерну. Це дозволяє робити "стрибки" по тексту, пропускаючи групи символів, які точно не збігаються.
* **Складність:** $O(N + M)$ у гіршому випадку, $O(N/M)$ у найкращому.

### 3. Алгоритм Рабіна-Карпа
Використовує хешування (rolling hash). Замість посимвольного порівняння він порівнює хеш-код патерну з хеш-кодом поточного фрагмента тексту. Якщо хеші збігаються, виконується додаткова перевірка символів (для уникнення колізій).
* **Складність:** $O(N + M)$ у середньому, але може бути повільнішим через арифметичні операції з хешами.

## ⚙️ Вимоги

* Python 3.x
* Бібліотеки: `timeit` (вбудована в Python, встановлення не потребує).


# Висновки щодо швидкості роботи алгоритмів пошуку підрядка

## 1. Аналіз для "Статті 1"

* **Найшвидший:** 🏆 **Алгоритм Боєра-Мура**.
    Він показав найкращий результат в обох сценаріях:
    * Пошук існуючого підрядка: `0.026 мс`
    * Пошук неіснуючого підрядка (повний прохід): `0.359 мс`

* **Розрив у швидкості:**
    * **Реальне слово:** Різниця не є критичною (Боєра-Мура у ~2-2.5 рази швидший за конкурентів).
    * **Вигадане слово:** Коли алгоритм змушений проходити весь текст, перевага Боєра-Мура стає колосальною — він у **4 рази** швидший за KMP та майже у **10 разів** швидший за Рабіна-Карпа.

* **Аутсайдер:** Алгоритм **Рабіна-Карпа** виявився найповільнішим у обох сценаріях.

---

## 2. Аналіз для "Статті 2"

* **Найшвидший:** 🏆 Знову **Боєра-Мура**.
    Тенденція зберігається: `0.169 мс` для реального слова та `0.519 мс` для вигаданого.

* **Масштабування:**
    Час пошуку зріс для всіх алгоритмів порівняно зі Статтею 1. Це свідчить про те, що Стаття 2 є більшою за обсягом або шукане слово знаходиться далі від початку тексту.

* **Ефективність:**
    Алгоритм Боєра-Мура демонструє стабільну ефективність на більшому обсязі даних. Розрив між ним та алгоритмом Рабіна-Карпа для неіснуючого слова знову досягає майже **9-кратного розміру** (`0.51 мс` проти `4.67 мс`).

---

## 3. Загальні висновки

На основі усереднених даних та динаміки тестів можна охарактеризувати кожен алгоритм наступним чином:

### 🥇 Боєра-Мура (Boyer-Moore) — Беззаперечний лідер
Це найефективніший алгоритм у даному тесті.
> **Чому:** Його механізм пропуску символів ("стрибків") дозволяє не перевіряти кожен символ тексту. Це особливо відчутно, коли збігу немає або текст дуже довгий. Чим довший шуканий підрядок, тим ефективніше він зазвичай працює.

### 🥈 Кнута-Морріса-Пратта (KMP) — "Золота середина"
Займає стабільне друге місце.
* Він значно швидший за Рабіна-Карпа, але поступається Боєра-Мура у **3-4 рази**.
* Його ефективність полягає в тому, що він не повертається назад по тексту, проте він змушений переглядати більше символів, ніж Боєра-Мура.

### 🥉 Рабіна-Карпа (Rabin-Karp) — Найповільніший
Показав найгірші результати у всіх тестах.
> **Причина:** Ймовірно, накладні витрати на хешування (або перерахунок хешу для кожного "вікна") у цій реалізації займають більше часу, ніж просте порівняння символів в інших алгоритмах. Цей алгоритм зазвичай програє при пошуку *одного* шаблону, але може бути ефективнішим при пошуку *кількох підрядків одночасно* (чого не перевірялося в цьому тесті).

---

### ✅ Фінальний вердикт
Для задач пошуку одиничного підрядка в текстових даних **алгоритм Боєра-Мура є найбільш оптимальним вибором** з точки зору швидкодії.